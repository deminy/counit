{
    "name": "deminy/counit",
    "description": "To run time/IO related unit tests (e.g., sleep function calls, database queries, API calls, etc) faster using Swoole.",
    "type": "library",
    "license": "MIT",
    "keywords": [
        "phpunit",
        "swoole"
    ],
    "require": {
        "phpunit/phpunit": "~12.5.24 || ~13.0"
    },
    "require-dev": {
        "swoole/ide-helper": ">=5.1"
    },
    "bin": [
        "counit"
    ],
    "suggest": {
        "ext-swoole": "To run time/IO related unit tests (e.g., sleep function calls, database queries, API calls, etc) faster."
    },
    "autoload": {
        "psr-4": {
            "Deminy\\Counit\\": "src"
        }
    }
}
