name: Coding Style Checks

on: [ push, pull_request, workflow_dispatch ]

jobs:
  ci:
    runs-on: ubuntu-24.04
    name: Syntax Checks

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Run Coding Style Checks
        run: docker run -q --rm -v "$(pwd):/project" -w /project -i jakzal/phpqa:php8.4-alpine php-cs-fixer fix --dry-run
